export default function EmployeePulseFormTest() {
  return (
    <div
      style={{
        maxWidth: 800,
        margin: "40px auto",
        padding: 24,
        color: "#E5E7EB",
        fontFamily:
          "system-ui, -apple-system, BlinkMacSystemFont, sans-serif",
      }}
    >
      <h1
        style={{
          fontSize: 24,
          fontWeight: 800,
          marginBottom: 8,
        }}
      >
        Employee Pulse – TEST FORM ROUTE
      </h1>
      <p style={{ fontSize: 14 }}>
        If you can see this page, the <code>/dashboard/employee-pulse-form</code>{" "}
        route is wired up correctly.
      </p>
    </div>
  );
}
